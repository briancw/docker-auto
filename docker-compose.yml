services:
    auto:
        container_name: auto
        image: auto
        build:
            dockerfile: Dockerfile
        volumes:
            - ./data:/home/auto/data
            - ./outputs:/home/auto/sd/outputs
        ports:
            - 7860:7860
        command:
            - python
            - launch.py
            - --listen
            - --xformers
            - --data-dir=/home/auto/data
            - --embeddings-dir=/home/auto/data/embeddings
            - --allow-code
            - --enable-insecure-extension-acces
        deploy:
            resources:
                reservations:
                    devices:
                        - driver: nvidia
                          capabilities: [gpu]
                          device_ids: ['0']